import { CommonMiddleware } from '../../common/middleware/common.middleware.config';

class SubProjectActivityMiddleware extends CommonMiddleware {}

export default new SubProjectActivityMiddleware();
